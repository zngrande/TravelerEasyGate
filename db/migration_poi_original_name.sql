-- ============================================================
-- 景點資料庫: 新增「原文地名」欄位
-- 放在 useful_travel_schema.sql 之後執行即可。
--
-- 用途: AI 解析行程時, 抽取出來的景點命名有時跟旅行社資料庫裡建立的名稱不完全一樣
-- (例如資料庫存的是「新穗高高空纜車」, 行程裡解析出來的是「新穗高纜車」或原文「新穂高ロープウェイ」),
-- 導致比對不到既有資料、又重新打一次 Google Maps 定位。開放填寫原文/別名後, 名稱搜尋與比對時
-- 一併比對這個欄位, 提高命中率。
-- ============================================================

ALTER TABLE poi ADD COLUMN original_name VARCHAR(500) DEFAULT NULL AFTER name;
