# 行程輸出範本製作規格

給旅行社準備自己 Word 範本 (.docx) 用。系統會把下面這些標記換成實際行程資料，
其他文字、字型、顏色、Logo、頁首頁尾都會照你原本的範本樣式保留。

## 1. 一次性欄位（文件裡任何地方都可以用，出現一次就換一次）

| 標記 | 會換成 |
|---|---|
| `{{title}}` | 行程標題 |
| `{{country}}` | 國家/地區 |
| `{{days_count}}` | 天數 |
| `{{date_range}}` | 出發～回程日期，格式 2026/03/01 ~ 2026/03/05 |

## 2. 圖片區塊（放在標題和每日行程之間）

在你想要放圖片的地方，**單獨一個段落**，裡面只打：

```
{{images_block}}
```

系統會把這一段換成這趟行程所有景點照片，**每張照片下方自動附上名稱**。
如果這趟行程完全沒有照片，這個段落會被整個移除，不會留空白。

## 3. 每日行程區塊（會依實際天數自動重複）

用兩個各自獨立的段落當「起點」跟「終點」標記：

```
{{day_start}}
```

... 中間放你想要的一天份版面（可以是表格、可以是段落，隨便設計）...

```
{{day_end}}
```

系統會把 `{{day_start}}` 和 `{{day_end}}` 中間的內容，當成「一天的版面樣式」，
依這趟行程實際天數複製多份（第1天、第2天...），並把裡面出現的以下標記
換成當天資料：

| 標記 | 會換成 |
|---|---|
| `{{day.number}}` | 第幾天（數字） |
| `{{day.title}}` | 當天標題（自動把當天所有景點名稱串起來） |
| `{{day.content}}` | 當天景點行程內容（條列成一整段文字） |
| `{{day.meals}}` | 當天餐食安排 |
| `{{day.hotel}}` | 當天住宿 |
| `{{day.map}}` | 當天景點連線地圖圖片（沒設定地圖功能或抓不到座標，這個段落會整段移除，不留空白） |

**範例**：如果你的範本長這樣（一個表格）：

```
{{day_start}}
+------------+----------------------------------+
| 第{{day.number}}天 | {{day.title}}              |
+------------+----------------------------------+
| {{day.content}}                                |
+------------+----------------------------------+
| 餐食: {{day.meals}} | 住宿: {{day.hotel}}       |
+------------+----------------------------------+
| {{day.map}}                                    |
+------------+----------------------------------+
{{day_end}}
```

系統會依天數把這整個表格複製對應份數，各自帶入該天的資料。

## 4. 逐項目區塊（選用，放在每日行程區塊裡面）

如果 `{{day.content}}`（全部景點擠成一整段文字）不夠用，想要每個景點自己獨立
一列/一格（例如要分開排版、加框線），可以改用 `{{item_start}}` / `{{item_end}}`，
放在 `{{day_start}}`...`{{day_end}}` 裡面的表格儲存格中：

```
{{item_start}}
【{{item.name}}】{{item.note}}
{{item.description}}
{{item.route}}
{{item_end}}
```

系統會依這一天實際的景點數量，把 `{{item_start}}` 和 `{{item_end}}` 中間的內容
複製對應份數，各自帶入該景點的資料（餐食、住宿不算在內，只有景點/亮點類項目
才會列進來），裡面可以用：

| 標記 | 會換成 |
|---|---|
| `{{item.name}}` | 景點/項目名稱 |
| `{{item.note}}` | 這個項目的備註 |
| `{{item.description}}` | 景點資料庫裡的介紹說明（自訂項目沒有連結資料庫就會是空的） |
| `{{item.route}}` | 離開這個景點前往下一站的拉車距離/時間 |

`{{item_start}}`...`{{item_end}}` 是選用的，跟 `{{day.content}}` 兩種寫法擇一使用即可，
沒放 `{{item_start}}` 完全不影響 `{{day.content}}` 正常運作。

## 注意事項

- `{{day_start}}` / `{{day_end}}` / `{{images_block}}` 這三個標記，
  每份範本裡**只能各出現一次**，而且該段落**只能有這個標記**，不要跟其他文字混在同一段。
- `{{item_start}}` / `{{item_end}}` 是放在 `{{day_start}}`...`{{day_end}}` 裡面的巢狀標記，
  在同一個逐日版面裡**只能各出現一次**；沒有放這兩個標記也完全沒關係，系統會照原本的
  `{{day.content}}` 輸出。
- 上傳範本後建議用系統的「預覽」功能實際看一次輸出結果，確認排版跟預期一致
  （尤其是表格框線、跨頁狀況）。
- 目前不支援頁首/頁尾裡放逐日標記，逐日區塊請放在內文（正文）範圍。